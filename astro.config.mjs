// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://polymath-sys.pages.dev', // Placeholder for production URL
  output: 'static',
  build: {
    format: 'directory'
  }
});
